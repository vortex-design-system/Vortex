{
  "name": "vortex",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@rollup/*"
    ]
  },
  "scripts": {
    "clean": "lerna run clean && lerna clean --yes && rimraf node_modules"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2"
  }
}
